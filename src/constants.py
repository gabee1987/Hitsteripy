import os

BACKGROUND_IMAGE_FILENAME = "card_bg_09.png"
# Note: Use path_utils.get_asset_path() to get the correct path
# This constant is kept for compatibility but path resolution should use path_utils
BACKGROUND_IMAGE_PLACEHOLDER = f"../assets/{BACKGROUND_IMAGE_FILENAME}"